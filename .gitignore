# Android / Gradle
android/build/
android/app/build/
android/.gradle/
android/local.properties
*.iml
.idea/

# Node (only used by the headless UI harness)
node_modules/

# Signing material — never commit these
*.jks
*.keystore
signing.properties
android/keystore.properties

<?xml version="1.0" encoding="utf-8"?>
<!-- Nothing is backed up: the save is signed with a device-bound key, so a
     copy restored elsewhere would fail verification anyway. -->
<full-backup-content>
    <exclude domain="file" />
    <exclude domain="database" />
    <exclude domain="sharedpref" />
</full-backup-content>
